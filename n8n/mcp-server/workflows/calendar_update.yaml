# workflows/create_customer.yaml

name: calendar_update
description: Updates events in the calendar.

n8n:
  url: https://n8n.bendun.io/webhook/4c78d8b1-52a0-4200-8ef3-a2190206888a
  auth:
    type: header
    key: X-API-Key
    env: N8N_AUTH_TOKEN

input:
  type: object
  properties:
    eventid:
      type: string
      description: ID of the calendar event to update.
    starttime:
      type: string
      description: Start time of the calendar event in the format 2026-08-22T01:00:00+02:00.
    endtime:
      type: string
      description: End time of the calendar event in the format 2026-08-22T01:00:00+02:00.
    summary:
      type: string
      description: Summary of the calendar event.
    description:
      type: string
      description: Description of the calendar event.
    rrule:
      type: string
      description: Recurrence rule of the calendar event in the format "FREQ=DAILY;INTERVAL=1".
    location:
      type: string
      description: Location of the calendar event.
    allday:
      type: boolean
      description: Whether the calendar event is an all-day event.
    attendees:
      type: string
      description: Attendees of the calendar event in the format "email@example.com,email2@example.com".
  required:
    - eventid

execution:
  timeout_seconds: 30

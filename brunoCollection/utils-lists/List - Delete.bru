meta {
  name: List - Delete
  type: http
  seq: 10
}

post {
  url: {{utils_lists_base_url}}/lists/delete
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "id": "{{list_id}}",
    "actor": "bruno"
  }
}

assert {
  res.status: eq 200
  res.body.list.id: isDefined
  res.body.audit_id: isDefined
}

script:post-response {
  const auditId = res.body?.audit_id;
  if (auditId) {
    bru.setVar("audit_id", auditId);
    bru.setVar("list_delete_audit_id", auditId);
  }
}
